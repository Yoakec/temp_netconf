{
  "VRRP_网关冗余": "!\ninterface Vlan-interface{{ vrrp_vlan }}\n ip address {{ vrrp_vip }} {{ vrrp_mask }}\n vrrp vrid {{ vrrp_group }} virtual-ip {{ vrrp_vip }}\n vrrp vrid {{ vrrp_group }} priority {{ vrrp_priority }}\n vrrp vrid {{ vrrp_group }} preempt-mode timer delay {{ vrrp_preempt_delay }}\n vrrp vrid {{ vrrp_group }} authentication-mode simple cipher {{ vrrp_auth_key }}\n!"
}
